apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: swe-agent-builder-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: 'https://github.com/opex-sa/Agentic-builder.git'
    targetRevision: prod
    path: k8s/overlays/prod
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: agentic-builder-prod
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
