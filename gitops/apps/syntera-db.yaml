apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: syntera-db
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: 'https://github.com/Obelion-ai/swe_syntera.git'
    targetRevision: main
    path: k8s/syntera-db/overlays/prod
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: syntera-db
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - RespectIgnoreDifferences=true
  ignoreDifferences:
    - group: postgresql.cnpg.io
      kind: Cluster
      jsonPointers:
        - /status
    - group: postgresql.cnpg.io
      kind: Pooler
      jsonPointers:
        - /status
